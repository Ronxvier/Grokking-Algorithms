## Divide & Conquer
1.  Figure out a simple case as a base case
2. Figure out how to reduce your problem and get to the base case

# Quicksort
1. Start by picking a *pivot* element from the array
2. Find elements smaller than the pivot and larger than the pivot, split them into two subarrays (*partitioning*)
3. Recursively repeat
